syntax = "proto3";

package kaisui;

// TextMessage compatible with ractor's BytesConvertable
message TextMessage {
  string body = 1;
}

// Wrapper for distributed-process compatibility
message KaisuiMessage {
  oneof payload {
    TextMessage text_message = 1;
  }
}
